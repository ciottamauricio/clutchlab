# Deploy + DORA instrumentation for the frontend. The directory is frontend/ but the
# metric records it as "web" — the name the deployments table and the dashboard use.
name: deploy-web

on:
  push:
    branches: [main]
    paths: ["frontend/**", ".github/workflows/deploy.web.yml"]

jobs:
  deploy:
    uses: ./.github/workflows/deploy.reusable.yml
    with:
      service: web
    secrets:
      DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
      DEPLOY_KEY: ${{ secrets.DEPLOY_KEY }}
      DORA_INGEST_TOKEN: ${{ secrets.DORA_INGEST_TOKEN }}
      DORA_API_URL: ${{ secrets.DORA_API_URL }}
