# Provisioned datasources — Grafana finds Loki (and later Jaeger) on boot, no clicking.
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
