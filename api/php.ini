; CS2 demos are large; the default 2M/8M CLI limits would reject uploads before
; Laravel ever sees them. nginx (client_max_body_size 1024M) is the outer gate.
upload_max_filesize = 1024M
post_max_size = 1024M
memory_limit = 1024M
max_execution_time = 120
