; CS2 demos are large; the default 2M/8M CLI limits would reject uploads before
; Laravel ever sees them. nginx (client_max_body_size 512M) is the outer gate.
upload_max_filesize = 512M
post_max_size = 512M
memory_limit = 512M
max_execution_time = 120
